## Code style

This project uses Preact, which is React with upgrades. NEVER USE className, ALWAYS USE class. Use `onInput` instead of `onChange`

This project uses UnoCSS, which is Tailwind with upgrades. ALWAYS GROUP VARIANTS e.g. `md:(one two three)
